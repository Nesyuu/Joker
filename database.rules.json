{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['code', 'hostId', 'phase', 'round', 'players', 'votes', 'log'])"
      }
    }
  }
}
